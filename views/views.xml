<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="computer_service.list">
      <field name="name">ComputerService list</field>
      <field name="model">computer_service.computer_service</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="computer_service.action_window">
      <field name="name">ComputerService window</field>
      <field name="res_model">computer_service.computer_service</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="computer_service.action_server">
      <field name="name">ComputerService server</field>
      <field name="model_id" ref="model_computer_service_computer_service"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="ComputerService" id="computer_service.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="computer_service.menu_1" parent="computer_service.menu_root"/>
    <menuitem name="Menu 2" id="computer_service.menu_2" parent="computer_service.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="computer_service.menu_1_list" parent="computer_service.menu_1"
              action="computer_service.action_window"/>
    <menuitem name="Server to list" id="computer_service" parent="computer_service.menu_2"
              action="computer_service.action_server"/>
    -->
  </data>
</openerp>
